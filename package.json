{
  "name": "request-mocker-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace root for request-mocker (core + electron)",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "start": "pnpm --filter @request-mocker/electron start",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@9.0.0"
}
