{
  "name": "request_mocker",
  "version": "1.0.0",
  "main": "dist/main.js",
  "description": "Simple OpenAPI mock server with Electron UI",
  "scripts": {
    "dev": "vite",
    "build": " vite build && tsc",
    "start": "electron .",
    "test": "npm run build && jest",
    "cypress": "cypress run"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "cypress": "^12.0.0",
    "electron": "^30.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "vue": "^3.5.17"
  }
}
